[project]
name = "cellsimilaritymodel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cmapy==0.6.6",
    "matplotlib==3.10.7",
    "numpy==2.3.4",
    "opencv-python==4.11.0.86",
    "scipy==1.16.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/las_model"]
